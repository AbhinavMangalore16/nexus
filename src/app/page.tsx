import { Button } from "@/components/ui/button";
import { AuthDisplay } from "@/features/auth/components/auth-display";

export default function Home() {
  return (
   <div>
    <AuthDisplay/>
   </div>
  );
}
